#!/bin/bash

if [[ ! `isgit` = "true" ]]; then
    exit 1
fi

MAIN=`main`
REMOTE="origin"

git stash -u && \
  git checkout $MAIN && \
  git fetch && \
  git reset --hard $REMOTE/$MAIN && \
  echo "done" && \
  exit 0;

exit 1
