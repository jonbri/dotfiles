#!/bin/bash

if [[ ! `isgit` = "true" ]]; then
    exit 1
fi

MAIN=`main`

git stash -u && \
  git checkout $MAIN && \
  git fetch && \
  git reset --hard origin/$MAIN && \
  echo "done" && \
  exit 0;

exit 1
