#!/bin/bash
# ticker - install ticker-log in a Nova playpen

INPUT_FILE="$DOTFILES_DIR/bin/bin/preview-head.html"
OUTPUT_FILE="$NC/src/nova-commons/.storybook/preview-head.html"

cp $INPUT_FILE $OUTPUT_FILE

cat << EOF
Done. Bounce "yarn start".

// @ts-ignore
ticker.print('hello');
EOF

