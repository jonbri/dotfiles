#!/bin/bash
# mendr - Amend changes to HEAD commit and push to Gerrit as review

topic=`determine-topic`
if [ -z "$topic" ]; then
    git mend && git push origin HEAD:refs/for/master
else
    git mend && git push origin HEAD:refs/for/master/$topic
fi
