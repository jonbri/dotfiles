#!/bin/bash
# sow

if test -n "$1"; then
  SHA=$1
elif test ! -t 0; then
  while read line
  do
    SHA="$line"
    break
  done < "${1:-/dev/stdin}"
else
  SHA="HEAD"
fi

git show $SHA --format=raw

