#!/bin/bash
# gpom - git push origin $MASTER_OR_MAIN

IS_MASTER=`git br | grep -e " master$" | tr -d " *"`
if [ "$IS_MASTER" = "master" ]; then
  echo "detected master branch..."
  BRANCH=master
else
  echo "detected main branch..."
  BRANCH=main
fi

git push origin $BRANCH

