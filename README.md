# dotfiles

Run `install.pl` to get started

Packages to install:
```
sudo apt-get install tree gitk gitg tmux vim vim-gnome chromium-browser filezilla lftp clementine vpnc remmina* openssh-server openssh-client midori audacity g++ curl libssl-dev apache2-utils git-core rig make
```